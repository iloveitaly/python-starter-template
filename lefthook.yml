prepare-msg-hook:
  commands:
    aiautocommit:
      run: aiautocommit commit --output-file "$1"
      interactive: true
      env:
        LEFTHOOK: 0
        LOG_LEVEL: info
      # skip:
      #   - merge
      #   - rebase
      # run: "aiautocommit commit --output-file \"$1\"":
      only:
        run: which aiautocommit > /dev/null

pre-push:
  parallel: true
  scripts:
    "just py_lint":
    "just js_lint":