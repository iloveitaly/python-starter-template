# tools shared between dev and ci should be specified here. This makes it easy to specify exact versions.
# if tools are dev only, they should be installed via brew or some other method.

# exact versions should always be specified so there if something breaks, we know exactly which version
# it used to previously work under.

just 1.46.0
direnv 2.37.1

uv 0.9.26
python 3.14.2

# language + package manager versions required for the web project
pnpm 10.28.1

# it's best to use a LTS version of node. Many packages (such as better-sqlite3) precompile binaries for LTS
# node versions. Ensuring the right packages, etc are available during container creation for these packages is a pain.
# https://nodejs.org/en/about/previous-releases
node 24
