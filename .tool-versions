# tools shared between dev and ci should be specified here. This makes it easy to specify exact versions.
# if tools are dev only, they should be installed via brew or some other method.

# this *should* be compatible with mise and asdf, but I'm only testing with mise

uv 0.8.10
just 1.42.4
direnv 2.37.1

# waiting on nixpacks to support pnpm 10.x
pnpm 9.15.9

# python and node versions must be tied to what is available in nixpgs
# https://search.nixos.org/packages?channel=24.11&show=nodejs_22&from=0&size=50&sort=relevance&type=packages&query=node

python 3.13.6
node 22.18.0
